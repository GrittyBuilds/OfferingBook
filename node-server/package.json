{
  "name": "muniment-server",
  "version": "2.0.0",
  "description": "Serves Muniment.html over http, for anyone who prefers a local server to opening the file. The application itself is the same single file.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "export-sqlite": "node src/export-sqlite.js"
  },
  "engines": {
    "node": ">=20"
  }
}
